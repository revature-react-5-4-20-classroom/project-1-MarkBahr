import React from 'react';
import { getUser } from '../api/ExpenseClient';
import { UserTable } from './UserTable';

interface IgetMyUserProps {

}

export class getMyUser implements IgetMyUserProps {

    render() {
        return (
            <div>
                <form>

                </form>
            </div>
        )
    }
}